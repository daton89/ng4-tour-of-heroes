// Inserire le impostazioni in questo file per sovrascrivere quelle utente e predefinite.
{
    "files.exclude": {
        "**/*.js": {
            "when": "$(basename).ts"
        },
        "**/*.js.map": true
    }
}